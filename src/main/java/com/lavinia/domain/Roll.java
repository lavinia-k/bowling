package com.lavinia.domain;

/**
 * Represents details about a single roll (ie. number of pins knocked down in that roll).
 */
public class Roll {

    public final Integer pins;

    public Roll(final Integer pins) {
        this.pins = pins;
    }
}
